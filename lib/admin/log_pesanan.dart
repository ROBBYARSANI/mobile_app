import 'package:flutter/material.dart';

class LogPesananPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Log Pesanan")),
      body: Center(child: Text("Tabel pesanan")),
    );
  }
}
